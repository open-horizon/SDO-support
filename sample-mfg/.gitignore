/sdo_device_binaries_linux_x64.tar.gz
/Services/
# keys/sdo.p12 is the runtime temporary place for the mfg private key so docker-compose will find it
/keys/sdo.p12
# keys/owner-key.pub is the runtime temporary place the owner public key is downloaded to, if the user specified a URL
/keys/owner-key.pub
