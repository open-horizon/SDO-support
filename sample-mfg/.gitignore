# For building the sdo_device_binaries_1.8_linux_x64 tar file
/sdo_device_binaries_1.8_linux_x64/
/sdo_device_binaries_1.8_linux_x64.tar.gz

# keys/manufacturer-keystore.p12 is the runtime temporary place for the mfg private key so docker-compose will find it
/keys/manufacturer-keystore.p12
# keys/owner-key.pub is the runtime temporary place the owner public key is downloaded to, if the user specified a URL
/keys/owner-key.pub

# We use ../sdo/supply-chain-tools-v1.8.0/docker_manufacturer/docker-compose.yml as-is, so we copy it here before docker-compose build
/docker-compose.yml
